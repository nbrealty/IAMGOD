/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

// Injected at build time (vite `define`) — the git short sha of the running build.
declare const __BUILD_ID__: string;
